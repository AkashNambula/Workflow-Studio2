apiVersion: v1
kind: Service

metadata:
  name: workflow-frontend-service

spec:
  selector:
    app: workflow-frontend

  ports:
    - protocol: TCP
      port: 4173
      targetPort: 4173

  type: NodePort