apiVersion: v1
kind: Service

metadata:
  name: workflow-backend-service

spec:
  selector:
    app: workflow-backend

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort